
type Dog {
  id: ID! @id
  name: String! @unique
  breed: String!
  owner: Owner! 
}

type Owner {
  id: ID! @id
  email: String! @unique
  name: String!
  personalPhone: String!
  workPhone: String!
  workId: String!
  dogs: [Dog!]!
}